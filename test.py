import connect
import pytest
from _pytest.reports import BaseReport


class Test:
    def test_1(self):
        #assert connect.validate() == connect.validate()
        assert connect.validate() == connect.validate()